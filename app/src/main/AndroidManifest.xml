<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mybarber">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBarber">
        <activity android:name=".Activities.profileActivity" />
        <activity android:name=".Activities.managerActivity" />
        <activity android:name=".Activities.allHaircutActivity" />
        <activity android:name=".Activities.addHaircutActivity" />
        <activity android:name=".Activities.registerActivity" />
        <activity android:name=".Activities.loginActivity" />
        <activity android:name=".Activities.allAppointmentActivity" />
        <activity android:name=".Activities.addAppointmentActivity" />
        <activity android:name=".Activities.selectAppointmentActivity" />
        <activity android:name=".Activities.appointmentsHistoryActivity" />


        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyBarber.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>